// Temporary manual phase override for testing the macro cycle without waiting
// hours for a natural transition. Flip to false (or delete the UI block that
// reads it) before shipping.
export const SHOW_DEBUG_PHASE = true;
